# Redis 密码（必须修改！与 cloudreve/conf.ini [Redis] 段密码一致）
# 建议用随机密码：openssl rand -base64 32
REDIS_PASSWORD=

# Aria2 RPC secret token.
# Must match the RPC secret configured in Cloudreve admin panel (Node settings).
ARIA2_RPC_SECRET=